package io.nanovc.agentsim;

/**
 * A base class for environment persister configs.
 */
public abstract class EnvironmentPersisterConfigBase implements EnvironmentPersisterConfigAPI
{
    // NOTE: There is no assumed configuration for all persisters. Sub classes should define implementation specific configuration settings.
}
