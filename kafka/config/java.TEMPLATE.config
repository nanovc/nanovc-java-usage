# This is an example of the java.config file that you should populate
# Follow these instructions to create this file: https://docs.confluent.io/current/tutorials/examples/clients/docs/java.html
# DO NOT COMMIT SENSITIVE CREDENTIALS to the code repo.
# The java.config file is explicitly ignored from git to avoid this situation, but you do need to have a java.config file there.

# Required connection configs for Kafka producer, consumer, and admin
bootstrap.servers=xxx.af-south-1.aws.confluent.cloud:9092
security.protocol=SASL_SSL
sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule   required username='XXX'   password='XXX';
sasl.mechanism=PLAIN
# Required for correctness in Apache Kafka clients prior to 2.6
client.dns.lookup=use_all_dns_ips
# Best practice for Kafka producer to prevent data loss
acks=all
